# interactive-colorization
Main repository for the Bachelor's thesis project: Interactive multi-variant colorization of images and videos. Contains submodules for the ML engine, UI, and LaTeX documentation.

```bash
train model=mamba

inference model=mamba input_dir=checkpoints/

evaluate model=mamba
```